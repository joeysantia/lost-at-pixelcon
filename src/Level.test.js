

describe("Level", () => {
    //unit tests
    it.skip("renders an image and areas", () => {

    })

    it.skip("generates an EnterName component when a player wins", () => {

    })

    //integration tests
    it.skip("generates a Dropdown element when the image is clicked", () => {
        
    })

})